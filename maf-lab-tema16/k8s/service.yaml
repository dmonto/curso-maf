apiVersion: v1
kind: Service
metadata:
  name: maf-support-agent-service
  namespace: maf-agents
  labels:
    app: maf-support-agent
spec:
  type: ClusterIP
  selector:
    app: maf-support-agent
  ports:
    - name: http
      port: 80
      targetPort: 8000